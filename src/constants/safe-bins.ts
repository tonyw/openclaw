/**
 * Default safe binaries for command execution.
 * This is a constant-only module to avoid circular dependency issues.
 */
export const DEFAULT_SAFE_BINS = ["jq", "cut", "uniq", "head", "tail", "tr", "wc"];
